# 검증 현황 — 2026-09-25 (v2 통합)

v2는 bootstrap-v2 위의 **PRD 1.1.0 / 계약 1.1.0** 게임이다. snapshot의 `implementation`은 `game`이다. v1(main `1a1aff2`) 다음에 `integration/v2`에서 PR #14–#17을 통합했고, `release/v2`에서 이야기 작업(PR #18)을 더했다. 각 역할의 세부 증거는 `docs/handoffs/A|B|C/`에 있다.

## v1 이후 달라진 것

- **판단 단서(M1, 계약 1.1.0)**
  - 물건을 45° 이상 돌리면 내구 성향이 공개된다.
  - 안전 압력을 넘으면 긴장도(`stress01`)가 오르고, 계기판이 붉어지고 바늘과 물건이 떨린다.
  - 누르는 동안 예상 크기와 "들어가요/안 들어가요"가 보인다.
  - 보관물은 확정 상태로 기록된다. 그래서 새 renderer도 외형을 복원하고, 결과 화면에 물건별 내역이 나온다.
  - 결과 화면에 1L당 점수와 기기에 저장되는 최고 기록이 나온다.
- **작업대 UI(#17):** 반투명 작업대 HUD, 에스코어드림·리디바탕 번들, 두 줄 조작부.
- **이야기(#18)**
  - 영문 워드마크(Cafe24 PRO SLIM Max)
  - 도입 교신 3장면: 왜 부품을 누르는지 설명한다.
  - 윤서·도현 포즈 4종
  - 실제로 담은 것에 맞춘 귀환 교신

## 확인 결과

| 항목 | 실제 결과 | 한계 |
|---|---|---|
| 전체 검사 | Node 26.8.2 / npm 11.19.1에서 `npm run check` 통과: 고정 23파일, strict TypeScript, AST 모듈 경계, 16파일 103테스트, production build | 자동 검사일 뿐이며 실기기·시각 판정을 대신하지 않는다 |
| 게임 흐름 | 헤드리스 Chromium에서 실제 앱을 진행했다. 제목 → 교신 3장면 → 프레스 앞에 서기 → 실제 마우스로 돌려 성향 공개 → 누르기(삐걱 경고, 예상 크기) → 정착 결과 → 세 개 보관 → 결과별 귀환 교신 → 결과표(내역, 1L당 점수, 새 기록) → 재시작. 실패와 폐기 흐름도 확인했다. 브라우저 오류 0 | 데스크톱 Chromium이다. 터치와 iPhone Safari는 아니다 |
| 화면 배치 | 390×844와 375×812에서 HUD가 압력계와 케이스를 가리지 않는다. 넓은 가로 화면은 측면 패널로 배치된다. 도입 교신은 320×568, 768×1024, 1440×900, 844×390에서도 검사했다(C `story-onboarding/report.json`) | viewport 에뮬레이션 결과다 |
| 글꼴 | 에스코어드림(원본 그대로 재배포 허용), 리디바탕(SIL OFL 1.1), Cafe24 PRO SLIM Max(SIL OFL 1.1). 원본 파일을 수정하지 않고 번들했고 라이선스 파일도 함께 배포한다. 시작 화면에서 출처를 볼 수 있다 | 외부 CDN은 쓰지 않는다 |
| 제출 빌드 | `npm run build` 산출물 약 17.4MB(20MB 예산 이내). 루트 `index.html`과 상대 경로를 쓴다. ZIP을 하위 경로에서 열어 로드와 플레이를 확인했다 | itch.io 업로드와 대회 제출은 사용자가 한다 |

## 남은 조건

- 이야기 도중 GPU context를 강제로 잃게 한 뒤 복구하면, 모델 재로딩이 가끔 `loading`에 머문다(C `story-onboarding/retry-timeout.json`). 원인은 확정되지 않았다.
- 실제 iPhone Safari의 터치·safe-area·성능은 미검증이다. B의 이전 기기 측정은 40fps / P95 29ms로 60fps 목표에 미달했다.
- B 아트 판정의 미달 항목(구도, 작은 화면 손상 식별, 재질 세부)이 남아 있다.
- JS 단일 chunk가 Vite의 500kB 권고를 넘는다. 빌드는 성공한다.
- 대회 확인 항목은 아직 확인되지 않았다: AI 사용 허용·고지, 참가 자격, 웹 전달 인정. 오디오는 범위에서 제외했다.
